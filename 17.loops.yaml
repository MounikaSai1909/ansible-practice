- name: install packages
  hosts: web
  tasks:
    - name: installing packages
      ansible.builtin.dnf:
        name: "{{ item }}"        
        state: latest
      loop:
      - mysql
      - nginx
      - postfix