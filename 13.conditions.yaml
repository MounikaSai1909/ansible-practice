- name: conditions
  hosts: web
  become: yes
  tasks:
  - name: check user exists or not
    ansible.bulitin.command: id expense
    ignore_errors: TRUE
    
  - name: say Hello
    ansible.builtin.debug:
      msg: "Hello"